# Copy to .env.local for local dev (leave empty to use Vite proxy to localhost:8000)
VITE_API_URL=

# For production (Netlify env vars), set to your HF Space URL:
# VITE_API_URL=https://fynamicuz-fitter-backend.hf.space
